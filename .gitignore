# Internal working material — NOT for the public GitHub Pages site.
# Kept locally, never published: contains licensed third-party data
# (Coresignal-derived company data), competitor screenshots, and internal
# strategy/planning notes and homepage candidates.
/_internal/

# Hugo + Node build artifacts
node_modules/
/public/
/resources/
.hugo_build.lock

# Vite-built CSS (regenerated on every build)
/static/css/
